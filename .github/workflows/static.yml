# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  extract:
  # Single deploy job since we're just deploying
   name: Extract Action
  # You may pin to the exact commit or the version.
  # uses: ihiroky/extract-action@2bdc0b8926237c3829211a3e21ad129ce2769d05
   uses: https://raw.githubusercontent.com/ihiroky/extract-action/refs/heads/main/.github/workflows/release.yml@1
   with:
  # File path to extract.
    file_path: extract.zip
    # Directory path to extract archive.
    extract_dir: .
    # Shows details about the result of running this action.
    verbose: true
    
